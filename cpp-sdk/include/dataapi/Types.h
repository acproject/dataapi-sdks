#pragma once

// 包含所有类型定义文件
#include "types/CommonTypes.h"
#include "types/AiTypes.h"
#include "types/UserTypes.h"
#include "types/ProjectTypes.h"
#include "types/DatabaseTypes.h"
#include "types/WorkflowTypes.h"

// 为了向后兼容，保留原有的命名空间
namespace dataapi {
    // 所有类型定义现在都在各自的头文件中
    // 这个文件作为统一的入口点
}